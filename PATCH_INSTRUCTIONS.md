Replace this file in your repo:

- src/components/WelcomeModal.tsx

Then commit and push.

This update keeps the same sessionStorage behavior, but makes the modal much more compact on mobile so it fits on one screen more reliably.
