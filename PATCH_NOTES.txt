PATCH NOTES — Number Chain + wider Hidden Pair + longer Spot row

Changed files:
- src/app/play/[itemId]/_components/GameHost.tsx
- src/games/number-chain/NumberChainGame.tsx   (new)
- src/lib/competitiveRuns.ts
- src/lib/nextHiddenStateGames.ts
- src/lib/verifiedGames.ts
- src/games/hidden-pair-memory/HiddenPairMemoryGame.tsx

What this patch does:
1. Replaces the Clue Ladder frontend with Number Chain while keeping the same underlying game key ("clue-ladder"), so existing mapped items do not need DB remapping.
2. Replaces the clue/target banks behind clue-ladder with number-sequence content for both local/progressive-run and verified/hidden-state paths.
3. Makes Spot the Missing show a slightly longer initial row by increasing the shown token count by one in both competitive-run and verified challenge builders.
4. Adds pair-dispersion logic to Hidden Pair Memory so matching pairs are placed further apart more often, in both local/practice and verified/server challenge builders.

How to apply:
- Unzip into the repo root, overwriting when prompted.
- Test locally.
- Commit and push.
- Deploy to Vercel when ready.