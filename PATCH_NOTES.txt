PwnIt patch: Spot the Missing + timed run countdown fixes

Files included:
- src/games/spot-the-missing/SpotTheMissingGame.tsx
- src/games/rapid-math-relay/RapidMathRelayGame.tsx

What this patch changes:
1. Spot the Missing countdown now ticks live instead of appearing static.
2. Spot the Missing now shows the original row first, then switches to the shortened row after a brief memorize phase.
3. Spot the Missing now times out properly when the countdown reaches zero.
4. Rapid Math Relay now uses the same live ticking timer pattern so timeout expiry is enforced properly there too.

Apply on top of the current repo/patch state and restart the dev server.
