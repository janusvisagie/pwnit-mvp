PwnIt follow-up hotfix (GitHub-baseline compatible)

This hotfix replaces:
- src/app/play/[itemId]/_components/GameHost.tsx

Why:
- The current GameHost.tsx in the repo baseline is corrupted/truncated.
- That can cause runtime failures such as:
  "Element type is invalid. Received a promise that resolves to: undefined."
- This replacement restores a clean exported React component and keeps the
  verified/practice game host flow intact.

Apply:
1. Unzip into the repo root.
2. Allow it to replace the existing GameHost.tsx.
3. Rebuild / restart dev server.

Notes:
- This is intended as a follow-up on top of the previous game patch.
- It does not undo the Progressive Mosaic / Clue Ladder backend work.
