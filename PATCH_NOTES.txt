PwnIt patch: autofocus + live-site notes

Changed files
- src/app/play/[itemId]/_components/GameHost.tsx
- src/games/rapid-math-relay/RapidMathRelayGame.tsx

What this patch does
1. Adds host-level autofocus handling so the newly available game control gets focus automatically.
2. Marks Rapid Math Relay's answer input as the preferred autofocus target.
3. Lets focus move from a clicked Start button into the active input without requiring another click.
4. Applies the host-level behavior across all games rendered through GameHost (inputs are preferred; otherwise the first active button/control is focused).

Apply on top of your current patched repo state.
Then restart npm run dev or rebuild on Vercel.
