PwnIt competitive-run type hotfix

Changed files:
- src/lib/competitiveRuns.ts

What this fixes:
- narrows reveal handling using current.game instead of gameKey so TypeScript can safely access progressive-mosaic/clue-ladder-only fields during Production build on Vercel.

