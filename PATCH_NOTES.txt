PwnIt patch: sync Number Chain label across home/item surfaces

Changed files:
- src/lib/gameRules.ts

What this fixes:
- The shared game metadata for backend key "clue-ladder" now uses the user-facing label "Number Chain"
  and an updated description.
- Home cards and item pages that call getGameLabel()/getGameMeta() will now show Number Chain
  instead of Clue Ladder.

Why this avoids repeat mismatches:
- The label is fixed at the shared metadata layer rather than in a one-off page component.
- Any surface already using gameRules.ts will stay aligned automatically.

Apply:
1. Replace the file in your repo with the one in this ZIP.
2. Commit, push, and redeploy.
3. Hard refresh or use an incognito tab to confirm the updated labels.
